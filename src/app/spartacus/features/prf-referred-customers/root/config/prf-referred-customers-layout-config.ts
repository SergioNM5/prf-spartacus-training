import { LayoutConfig } from '@spartacus/storefront';

export const prfReferredCustomersLayoutConfig: LayoutConfig = {
  layoutSlots: {
    TrainingPageTemplate: {
      slots: ['BodyContent'],
    },
  },
};
